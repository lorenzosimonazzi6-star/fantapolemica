{
  "rules": {
    "users": {
      "$uid": {
        ".read":  "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "leagues": {
      "$leagueId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
